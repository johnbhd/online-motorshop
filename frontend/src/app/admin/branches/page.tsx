"use client";import {useMemo,useState} from "react";import {adminBranches} from "@/lib/mock/admin";import {AdminBadge} from "@/components/admin/AdminDataTable";export default function Branches(){const [query,setQuery]=useState("");const visible=useMemo(()=>adminBranches.filter(b=>`${b.name} ${b.address}`.toLowerCase().includes(query.toLowerCase())),[query]);return <div className="space-y-5"><section className="flex flex-col gap-4 xl:flex-row xl:items-end xl:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-orange-600">Branch Management</p><h2 className="mt-2 text-2xl font-bold tracking-tight text-[#0B1930] sm:text-3xl">ALD Branches</h2><p className="mt-2 text-sm text-slate-600 sm:text-base">Review store pickup availability, staff assignments, and branch information.</p></div><button className="min-h-11 rounded-lg bg-orange-500 px-4 text-sm font-semibold text-white">+ Add Branch</button></section><section className="grid grid-cols-1 gap-4 md:grid-cols-3">{[["3","Total Branches","Active store locations"],["3","Pickup Available","All branches offer pickup"],["9","Active Staff","Assigned across branches"]].map(([value,label,desc])=><article key={label} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-3xl font-bold text-[#0B1930]">{value}</p><h3 className="mt-1 font-semibold text-[#0B1930]">{label}</h3><p className="mt-1 text-sm text-slate-500">{desc}</p></article>)}</section><section className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><input value={query} onChange={e=>setQuery(e.target.value)} className="min-h-11 w-full max-w-md rounded-lg border border-slate-300 px-3 text-sm" placeholder="Search branches or address"/></section><section className="grid gap-5">{visible.map(branch=><article key={branch.name} className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><header className="flex flex-col gap-3 border-b border-slate-100 px-5 py-4 sm:flex-row sm:items-center sm:justify-between sm:px-6"><h2 className="text-lg font-semibold text-[#0B1930]">{branch.name}</h2><AdminBadge>{branch.status}</AdminBadge></header><div className="p-5 sm:p-6"><div className="grid grid-cols-1 gap-x-6 gap-y-5 md:grid-cols-2 xl:grid-cols-3"><Info label="Address" value={branch.address}/><Info label="Contact" value={branch.contact}/><Info label="Assigned Staff" value={`${branch.staff} Staff`}/><Info label="Operating Hours" value="Configured"/><Info label="Pickup" value={branch.pickup?"Store Pickup Available":"Store Pickup Unavailable"}/></div><footer className="mt-6 flex flex-col gap-3 border-t border-slate-100 pt-5 sm:flex-row sm:items-center sm:justify-between"><span className="text-sm font-medium text-emerald-700">⌂ Store Pickup Available</span><span className="flex gap-4"><button className="min-h-10 rounded-lg bg-orange-500 px-4 text-sm font-semibold text-white">Edit Branch</button><button className="text-sm font-semibold text-orange-600">View Staff</button></span></footer></div></article>)}{!visible.length&&<p className="rounded-xl border border-slate-200 bg-white p-12 text-center text-sm text-slate-500">No branches found.</p>}</section></div>};function Info({label,value}:{label:string;value:string}){return <div><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">{label}</p><p className="mt-1 break-words text-sm font-medium text-[#0B1930]">{value}</p></div>}
